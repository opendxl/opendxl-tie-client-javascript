/**
 * @module GtiAttrib
 * @description Attributes associated with `reputations` returned by the Global
 * Threat Intelligence (GTI) `reputation provider`.
 *
 * | Name              | Numeric | Description |
 * | ------------------| ------- | ----------- |
 * | ORIGINAL_RESPONSE | 2120340 | The raw response as returned by the Global Threat Intelligence (GTI) `reputation provider`. |
 *
 * @private
 */

'use strict'

module.exports = {
  /**
   * The raw response as returned by the Global Threat Intelligence (GTI)
   * `reputation provider`.
   */
  ORIGINAL_RESPONSE: '2120340'
}
